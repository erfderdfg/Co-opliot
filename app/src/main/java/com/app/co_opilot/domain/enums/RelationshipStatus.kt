package com.app.co_opilot.domain.enums

enum class RelationshipStatus {
    // Keeping it simple with only two fields for now
    LIKED_BY_USER1,
    LIKED_BY_USER2,
    // MUTUAL, optional for both like each other
    // NONE, no relation
}